platform :ios, '8.0'

target 'Backand' do
  use_frameworks!

  # Pods for Backand
  pod 'Alamofire', '~> 4.4.0'
  pod 'SwiftKeychainWrapper', '~> 3.0.1'

  target 'BackandTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
